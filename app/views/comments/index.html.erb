
<table class="table table-hover">
  <thead class="thead-default">
    <tr>
      <td>title</td>
      <td>content</td>
      <td></td>
    </tr>
  </thead>
  <tbody>
    <% @comments.each do |comment| %>
    <tr>
      <td><%= comment.title %></td>
      <td><%= comment.content %></td>

    </tr>
    <% end %>
  </tbody>
</table>
